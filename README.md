# agromind

1. cd backend
2. python -m venv .venv
3. source .venv\Scripts\activate
4. pip install -r requirements.txt
5. uvicorn main:app --reload
6. In a separate terminal: cd frontend
7. npm install
8. npm run dev
